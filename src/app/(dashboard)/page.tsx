'use client'

import RulesList from "@/components/RulesList";

const Page = () => {
  return (
    <section className="w-full grid grid-cols-4 grid-rows-2 p-4">
      <RulesList />
    </section>
  );
};

export default Page;
